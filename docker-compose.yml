services:
  promos-activas:
    build: .
    container_name: promos-activas
    ports:
      - "6700:80"
    volumes:
      - promos_activas_data:/app/data
    restart: unless-stopped

volumes:
  promos_activas_data:
